# cristianandrei1423.github.io

My portfolio site, live at **https://cristianandrei1423.github.io**.

Plain HTML + CSS with no build step, hosted on GitHub Pages.

- `index.html` holds the content (projects, about, links)
- `style.css` holds the styling; colors and fonts are CSS variables at the top

To preview locally, open `index.html` in a browser.
